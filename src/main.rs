// This binary is a placeholder. Simulation scenarios are run via the `examples/`
// directory (e.g. `cargo run --example lorawan_file_transfer`).
fn main() {
    println!("theatron");
}

#[cfg(test)]
mod tests {
    #[test]
    fn main_does_not_panic() {
        super::main();
    }
}
